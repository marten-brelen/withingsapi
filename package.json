{
  "name": "withings-server",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel serverless functions for Withings OAuth and data.",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "lint": "echo \"No lint configured\"",
    "test": "echo \"No tests configured\""
  },
  "dependencies": {
    "@upstash/redis": "^1.34.9",
    "@lens-protocol/client": "latest",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "@vercel/node": "^5.5.27",
    "typescript": "^5.9.3"
  }
}
